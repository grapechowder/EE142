/*
* This file is used to provide stubs for the functions and types defined in the SFML library.
* We use it so that the project can be compiled and tested by zyBooks.
*
* DO NOT MODIFY THIS FILE!
*/

#include "Graphics.h"

#ifdef ZYBOOKS

// Need to define our colors somewhere in a .cpp file
const sf::Color sf::Color::Black;
const sf::Color sf::Color::White;
const sf::Color sf::Color::Red;
const sf::Color sf::Color::Green;
const sf::Color sf::Color::Blue;
const sf::Color sf::Color::Yellow;
const sf::Color sf::Color::Magenta;
const sf::Color sf::Color::Cyan;
const sf::Color sf::Color::Transparent;

#endif